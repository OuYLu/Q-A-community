<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.QaTopicCategoryMapper">
  <select id="selectCategoriesByTopicId" resultType="com.community.vo.TopicCategoryVO">
    SELECT
      c.id,
      c.name,
      c.parent_id AS parentId
    FROM qa_topic_category tc
    JOIN qa_category c ON c.id = tc.category_id
    WHERE tc.topic_id = #{topicId}
      AND c.delete_flag = 0
    ORDER BY c.sort ASC, c.id ASC
  </select>
</mapper>
