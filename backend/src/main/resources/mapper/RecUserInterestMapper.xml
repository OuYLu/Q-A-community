<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.RecUserInterestMapper">
  <insert id="upsertInterestDelta">
    INSERT INTO rec_user_interest (user_id, category_id, score, updated_at)
    VALUES (#{userId}, #{categoryId}, GREATEST(#{delta}, 0), NOW())
    ON DUPLICATE KEY UPDATE
      score = GREATEST(0, score + #{delta}),
      updated_at = NOW()
  </insert>
</mapper>
