<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.mapper.PermissionMapper">
  <select id="selectByRoleId" resultType="com.community.entity.Permission">
    SELECT p.*
    FROM role_permission rp
    JOIN permission p ON rp.permission_id = p.id
    WHERE rp.role_id = #{roleId}
    ORDER BY p.created_at DESC
  </select>

  <select id="selectMenusByUserId" resultType="com.community.entity.Permission">
    SELECT p.*
    FROM user_role ur
    JOIN role_permission rp ON ur.role_id = rp.role_id
    JOIN permission p ON rp.permission_id = p.id
    WHERE ur.user_id = #{userId}
      AND p.type = 'menu'
    ORDER BY p.parent_id ASC, p.sort ASC, p.id ASC
  </select>
</mapper>
